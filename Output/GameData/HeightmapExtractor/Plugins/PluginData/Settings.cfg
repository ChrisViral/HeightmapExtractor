SETTINGS
{
	//Height of the maps in pixels
	mapHeight = 1440
	//Width of the maps in pixels
	mapWidth = 2880
	//Number of pixels itterated per update cycle, stops at width
	maxPixelsPerFrame = 2880
	//Exact name of the bodies to extract, write "all" to extract them all
	bodies = all
	//If the maps will be extracted nest time the game runs
	extractMaps = true
	//Absolute URL of where the maps should be saved, leave empty for local "Maps" folder
	destinationURL =
}